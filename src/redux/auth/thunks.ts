import { createAsyncThunk } from "@reduxjs/toolkit";



export const startCreateUserNative = createAsyncThunk('auth/createNative', () => {

});

export const startLoginNative = createAsyncThunk('auth/login', () => {

});

export const startGoogleSignIn = createAsyncThunk('auth/google', () => {

});


